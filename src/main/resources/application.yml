spring:
  application:
    name: mopl
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

mopl:
  jwt:
    secret: ${JWT_SECRET}
    access-token-expiry-ms: 1800000
    refresh-token-expiry-ms: 604800000