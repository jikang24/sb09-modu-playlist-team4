spring:
  application:
    name: mopl
  profiles:
    active: local  # 기본은 로컬 환경으로 실행
jwt:
  secret: ${JWT_SECRET}
  access-token-expiry-ms: 1800000    # 30분
  refresh-token-expiry-ms: 604800000 # 7일
